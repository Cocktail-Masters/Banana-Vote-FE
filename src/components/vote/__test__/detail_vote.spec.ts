import { test, expect } from "@playwright/test";
import { browser } from "process";

test("vote_detail", async ({ page }) => {
  // await page.goto("/ko/vote/2");
  // const element = await page.locator(".mt-[25px] flex flex-col").first();
  // await element.isVisible();
  // await element.click();
  // await page.getByRole("button", { name: "투표하기" }).click();
  // await page.getByRole("button", { name: "결과 예측하기" }).click();
});
